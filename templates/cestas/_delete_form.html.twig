
<i class="text-inverse pr-2" data-toggle="tooltip" title="Borrar cesta" name="delete_detalle" id="{{ cesta.id }}">
                                                                <i class="ti-trash"></i>
                                                        </i> 


<form method="post" action="{{ path('cestas_delete', {'id': cesta.id}) }}" onsubmit="return confirm('¿Seguro que quieres borrar este ticket?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cesta.id) }}">
    <button class=" btn btn-outline-danger">Borrar</button>
</form>


