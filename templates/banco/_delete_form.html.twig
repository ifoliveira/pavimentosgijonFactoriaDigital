<form method="post" action="{{ path('banco_delete', {'id_Bn': banco.id_Bn}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banco.id_Bn) }}">
    <button class="btn">Delete</button>
</form>
