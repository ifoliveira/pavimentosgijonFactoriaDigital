{# templates/iapresupuesto/_styles_chat.html.twig #}

<style>

    /* ---------- BLOQUES PRINCIPALES ---------- */

    .pg-section-wrapper {
        max-width: 950px;
        margin: 40px auto;
        padding: 0 15px;
    }

    .pg-card {
        background: #ffffff;
        border-radius: 18px;
        padding: 30px;
        box-shadow: 0 10px 28px rgba(0,0,0,0.06);
    }

    /* ---------- SELECTOR ---------- */

    .pg-selector {
        text-align: center;
        margin-bottom: 40px;
    }

    .pg-title {
        font-size: 1.4rem;
        font-weight: 700;
        margin-bottom: 25px;
    }

    .pg-selector-buttons {
        display: flex;
        gap: 15px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .pg-btn {
        padding: 12px 18px;
        border-radius: 12px;
        border: none;
        font-weight: 600;
        cursor: pointer;
        transition: 0.2s ease;
    }

    .pg-btn-ducha {
        background: #e8f0ff;
        color: #1e40af;
    }

    .pg-btn-ducha:hover {
        background: #d7e6ff;
    }

    .pg-btn-completo {
        background: #e9f7ef;
        color: #17613b;
    }

    .pg-btn-completo:hover {
        background: #dff4e8;
    }

    /* ---------- CHAT ---------- */

    .pg-chat {
        margin-top: 20px;
        margin-bottom: 40px;
    }

    .pg-chat-messages {
        height: 350px;
        overflow-y: auto;
        padding: 15px;
        border-radius: 12px;
        background: #f8fafc;
        border: 1px solid #e0e7ff;
        margin-bottom: 15px;
    }

    .pg-chat-form {
        display: flex;
        gap: 10px;
    }

    .pg-chat-input {
        flex: 1;
        padding: 10px 14px;
        border-radius: 10px;
        border: 1px solid #cbd5e1;
    }

    .pg-chat-send {
        padding: 10px 18px;
        background: #2563eb;
        color: #fff;
        border: none;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
    }

    /* Mensajes */
    .chat-message {
        max-width: 80%;
        margin-bottom: 12px;
        padding: 10px 14px;
        border-radius: 14px;
        line-height: 1.4;
    }

    .chat-ai {
        background: #eceffd;
        color: #1e1b4b;
    }

    .chat-user {
        margin-left: auto;
        background: #2563eb;
        color: #fff;
    }

    .chat-system {
        background: #f1f5f9;
        font-style: italic;
        text-align: left;
    }

    /* ---------- GALERÍA ---------- */

    .pg-subtitle {
        font-size: 1.3rem;
        margin-bottom: 25px;
        font-weight: 700;
    }

    .pg-gallery-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 25px;
    }

    .pg-gallery-img {
        width: 100%;
        border-radius: 14px;
        box-shadow: 0 5px 18px rgba(0,0,0,0.08);
    }

</style>

<style>

/* ----------- CONTENEDOR DEL CHAT ----------- */

#chat-box {
    width: 100%;
    max-width: 900px;
    margin: 40px auto;
    background: #ffffff;
    border-radius: 22px;
    padding: 25px 30px;
    box-shadow:
        0 8px 20px rgba(0,0,0,0.05),
        0 2px 6px rgba(0,0,0,0.04);
    display: none;
}

/* Área donde caen los mensajes */
#chat-messages {
    height: 420px;
    overflow-y: auto;
    padding: 15px;
    border-radius: 16px;
    background: linear-gradient(180deg, #fafafa, #ffffff);
    border: 1px solid #f1f5f9;
    scroll-behavior: smooth;
}

/* Scroll suave y limpio */
#chat-messages::-webkit-scrollbar { width: 6px; }
#chat-messages::-webkit-scrollbar-thumb {
    background: #d0d7e2;
    border-radius: 10px;
}

/* ----------- BURBUJAS DE MENSAJE ----------- */

.chat-message {
    max-width: 78%;
    padding: 10px 14px;
    margin: 10px 0;
    border-radius: 14px;
    font-size: 0.95rem;
    line-height: 1.35;
    animation: fadeInUp 0.2s ease-out;
}

/* IA (lado izquierdo) */
.chat-ai {
    background: #eef2ff;
    border-left: 4px solid #6366f1;
    color: #1e1b4b;
}

/* Usuario (derecha) */
.chat-user {
    background: #2563eb;
    color: #fff;
    margin-left: auto;
    border-right: 4px solid #1e40af;
}

/* Sistema (bienvenida) */
.chat-system {
    background: #f1f5f9;
    color: #334155;
    border-left: 4px solid #94a3b8;
}

/* Animación suave */
@keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(8px); }
    100% { opacity: 1; transform: translateY(0); }
}

/* ----------- INPUT + BOTÓN ----------- */

#chat-form {
    margin-top: 18px;
    display: flex;
    gap: 10px;
}

#chat-input {
    flex-grow: 1;
    border-radius: 14px;
    border: 1px solid #cbd5e1;
    padding: 12px 16px;
    font-size: 1rem;
    transition: border-color 0.15s ease, box-shadow 0.15s ease;
}

#chat-input:focus {
    border-color: #7c3aed;
    box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.15);
}

.btn-chat-send {
    background: #4f46e5;
    color: white;
    padding: 12px 20px;
    border-radius: 14px;
    font-weight: 600;
    border: none;
    transition: all 0.15s ease;
}

.btn-chat-send:hover {
    background: #4338ca;
    transform: translateY(-2px);
}

/* Móvil */
@media (max-width: 600px) {
    #chat-box {
        padding: 18px;
    }
    #chat-messages {
        height: 360px;
    }
    .chat-message {
        max-width: 92%;
    }
}
</style>


<style>
/* HERO SECTION */
.hero-selector {
    display: flex;
    justify-content: center;
    margin-top: 50px;
    margin-bottom: 40px;
}

/* CARD CONTENEDOR */
.selector-card {
    width: 100%;
    max-width: 900px;
    background: #ffffff;
    padding: 40px 50px;
    border-radius: 24px;
    box-shadow:
        0 8px 30px rgba(0,0,0,0.05),
        0 2px 6px rgba(0,0,0,0.04);
    text-align: center;
}

/* TÍTULO */
.selector-title {
    font-weight: 700;
    font-size: 1.6rem;
    margin-bottom: 25px;
    color: #111827;
}

/* CONTENEDOR BOTONES */
.selector-buttons {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

/* BOTONES */
.selector-btn {
    padding: 14px 26px;
    border-radius: 14px;
    border: none;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.15s ease;
    min-width: 280px;
}

/* Estilos específicos */
.btn-ducha {
    background: #e8efff;
    color: #1d4ed8;
    border: 2px solid #c7d8ff;
}
.btn-ducha:hover {
    background: #d4e4ff;
    transform: translateY(-2px);
}

.btn-completo {
    background: #e7f7ee;
    color: #047857;
    border: 2px solid #bfead1;
}
.btn-completo:hover {
    background: #d2f1e2;
    transform: translateY(-2px);
}

/* RESPONSIVE */
@media (max-width: 600px) {
    .selector-card {
        padding: 30px 24px;
    }
    .selector-btn {
        min-width: 100%;
    }
}

.pdf-button {
    display: inline-block;
    background: linear-gradient(135deg, #0073e6, #005bb5);
    color: white;
    padding: 12px 20px;
    margin-top: 10px;
    margin-bottom: 10px;
    font-size: 15px;
    border-radius: 12px;
    font-weight: 600;
    text-align: center;
    cursor: pointer;
    box-shadow: 0 3px 8px rgba(0,0,0,0.20);
    transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.pdf-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(0,0,0,0.25);
}

.pdf-button:active {
    transform: scale(0.98);
}

/* CARD GENERAL */
.pdf-card {
    background: #ffffff;
    padding: 20px;
    margin: 20px auto 0 auto;
    border-radius: 12px;
    max-width: 340px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.12);
    animation: fadeIn 0.3s ease;
}

/* TÍTULO */
.pdf-title {
    text-align: center;
    margin-bottom: 15px;
    font-size: 20px;
    font-weight: 700;
    color: #333;
}

/* GRUPO DE CAMPOS */
.pdf-field {
    display: flex;
    flex-direction: column;
    margin-bottom: 15px;
}

/* LABEL */
.pdf-field label {
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 6px;
    color: #444;
}

/* INPUT */
.pdf-field input {
    padding: 10px 12px;
    font-size: 15px;
    border-radius: 8px;
    border: 1px solid #ccc;
    transition: all 0.2s ease;
    outline: none;
}

.pdf-field input:focus {
    border-color: #0073e6;
    box-shadow: 0 0 0 2px rgba(0,115,230,0.2);
}

/* BOTÓN SUBMIT */
.pdf-submit {
    width: 100%;
    background: linear-gradient(135deg, #0073e6, #005bb5);
    color: white;
    padding: 12px;
    font-size: 16px;
    border: none;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    text-align: center;
    transition: 0.2s ease;
}

.pdf-submit:hover {
    background: linear-gradient(135deg, #0088ff, #006ad1);
    transform: translateY(-2px);
}

.pdf-submit:active {
    transform: scale(0.97);
}

/* ANIMACIÓN */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0px); }
}

/* MODAL OVERLAY */
.pdf-modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.55);
    display: flex;
    justify-content: center;
    align-items: center;   /* 🔥 modal centrado vertical y horizontal */
    padding: 20px;
    z-index: 9999;
    animation: fadeIn 0.3s ease;
}


/* MODAL CARD */
.pdf-modal-card {
    width: 100%;
    max-width: 380px;
    background: #fff;
    border-radius: 16px;
    padding: 25px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.25);
    animation: slideIn 0.35s ease forwards;
    position: relative;
}

/* CLOSE BUTTON */
.pdf-close-btn {
    background: transparent;
    border: none;
    font-size: 20px;
    color: #777;
    position: absolute;
    right: 15px;
    top: 15px;
    cursor: pointer;
}

.pdf-close-btn:hover {
    color: #000;
}

/* FIELDS */
.pdf-field {
    margin-bottom: 16px;
    display: flex;
    flex-direction: column;
}

.pdf-field label {
    margin-bottom: 6px;
    font-weight: 600;
    color: #444;
    font-size: 14px;
}

.pdf-field input {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 15px;
    outline: none;
    transition: 0.2s;
}

.pdf-field input:focus {
    border-color: #0073e6;
    box-shadow: 0 0 0 2px rgba(0,115,230,0.2);
}

/* ERROR STATE */
.pdf-error {
    color: #d00;
    font-size: 13px;
    margin-top: 3px;
    min-height: 16px;
}

/* CHECKBOX */
.pdf-check {
    flex-direction: row;
    align-items: center;
}

.pdf-check input {
    margin-right: 10px;
}

.pdf-check-label {
    font-size: 14px;
}

.pdf-submit {
    width: 100%;
    background: linear-gradient(135deg, #0073e6, #005bb5);
    color: white;
    padding: 12px;
    border: none;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: 0.2s ease;
}

.pdf-submit:hover {
    background: linear-gradient(135deg, #0088ff, #006ad1);
    transform: translateY(-2px);
}

/* ANIMACIONES */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideIn {
    from { transform: translateY(100px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

/* MODAL PRESUPUESTO */

.pg-modal.hidden { display:none; }

  .pg-modal{
    position:fixed; inset:0; z-index:9999;
    display:flex; align-items:center; justify-content:center;
  }
  .pg-modal__backdrop{
    position:absolute; inset:0; background:rgba(0,0,0,.6);
  }
  .pg-modal__panel{
    position:relative;
    width:min(720px, 94vw);
    max-height:90vh;
    background:#fff;
    border-radius:14px;
    padding:14px;
    box-shadow:0 10px 30px rgba(0,0,0,.25);
    text-align:center;
  }

    /* CONTENEDOR DEL CARRUSEL */
    .pg-carousel{
    position:relative;
    height:360px;
    overflow:hidden;
    border-radius:10px;
    margin-bottom:12px;
    }

    /* SLIDE */
    .pg-slide{
    position:absolute;
    inset:0;
    opacity:0;
    transition:opacity .6s ease;
    }

    /* SLIDE ACTIVO */
    .pg-slide.active{
    opacity:1;
    }

    /* IMAGEN DENTRO DEL SLIDE */
    .pg-slide img{
    width:100%;
    height:100%;
    object-fit:cover;
    display:block;
    }

    /* TEXTO SOBRE LA IMAGEN */
    .pg-slide-text{
    position:absolute;
    bottom:12px;
    left:12px;
    padding:8px 12px;
    background:rgba(0,0,0,.55);
    color:#fff;
    border-radius:6px;
    font-size:14px;
    }

    .pg-modal__text h3{ margin:6px 0 4px; font-size:18px; }
    .pg-modal__text p{ margin:0 0 10px; opacity:.85; }

    .pg-modal__actions.hidden{ display:none; }
    .pg-btn{
        width:100%;
        padding:12px 14px;
        border-radius:10px;
        border:0;
        cursor:pointer;
        font-size:16px;
        margin-top:10px;
        background:#0a7cff;
        color:#fff;
    }
    .pg-btn--ghost{
        background:#f2f2f2;
        color:#222;
    }

    .pg-carousel__calc{
    position:absolute; inset:0;
    background:linear-gradient(135deg,#0a7cff,#043a7a);
    color:#fff;
    display:flex;
    align-items:center;
    justify-content:center;
    text-align:center;
    opacity:0;
    transition:opacity .6s ease;
    }

    .pg-carousel__calc.active{
    opacity:1;
    }

    .pg-carousel__calc strong{
    display:block;
    font-size:22px;
    margin-bottom:6px;
    }
    .pg-carousel__calc span{
    font-size:14px;
    opacity:.9;
    }

    .pg-slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity .6s ease;
    }

    .pg-slide.active {
    opacity: 1;
    }

    .pg-slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    }

    /* TEXTO SOBRE LA IMAGEN */
    .pg-slide-text {
    position: absolute;
    left: 12px;
    bottom: 12px;
    padding: 8px 12px;
    background: rgba(0,0,0,.55);
    color: #fff;
    font-size: 14px;
    border-radius: 6px;
    }


</style>

