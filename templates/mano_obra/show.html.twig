{% extends 'base.html.twig' %}

{% block title %}ManoObra{% endblock %}

{% block body %}
    <h1>ManoObra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mano_obra.id }}</td>
            </tr>
            <tr>
                <th>TipoMo</th>
                <td>{{ mano_obra.tipoMo }}</td>
            </tr>
            <tr>
                <th>TextoMo</th>
                <td>{{ mano_obra.textoMo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mano_obra_index') }}">back to list</a>

    <a href="{{ path('mano_obra_edit', {'id': mano_obra.id}) }}">edit</a>

    {{ include('mano_obra/_delete_form.html.twig') }}
{% endblock %}
