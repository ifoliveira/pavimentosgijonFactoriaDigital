{% extends 'base.html.twig' %}

{% block title %}Tipoproducto{% endblock %}

{% block body %}
    <h1>Tipoproducto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoproducto.id }}</td>
            </tr>
            <tr>
                <th>Decripcion_Tp</th>
                <td>{{ tipoproducto.decripcionTp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipoproducto_index') }}">back to list</a>

    <a href="{{ path('tipoproducto_edit', {'id': tipoproducto.id}) }}">edit</a>

    {{ include('tipoproducto/_delete_form.html.twig') }}
{% endblock %}
