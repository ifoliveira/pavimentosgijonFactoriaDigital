{% extends 'base.html.twig' %}

{% block title %}TipoManoObra{% endblock %}

{% block body %}
    <h1>TipoManoObra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_mano_obra.id }}</td>
            </tr>
            <tr>
                <th>TipoTm</th>
                <td>{{ tipo_mano_obra.tipoTm }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_mano_obra_index') }}">back to list</a>

    <a href="{{ path('tipo_mano_obra_edit', {'id': tipo_mano_obra.id}) }}">edit</a>

    {{ include('tipo_mano_obra/_delete_form.html.twig') }}
{% endblock %}
