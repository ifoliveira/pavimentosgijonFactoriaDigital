<form method="post" action="{{ path('app_catalogo_producto_delete', {'id': catalogo_producto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catalogo_producto.id) }}">
    <button class="btn">Delete</button>
</form>
