# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    c43Dir: "%kernel.project_dir%/public_html/C43"
    presupuestoDir: "%kernel.project_dir%/public_html/Presupuestos"
    ticketsDir: "%kernel.project_dir%/public_html/Tickets"
    nordigen: "%kernel.project_dir%/public_html/Nordigen"
    openai.api_key: '%env(OPENAI_API_KEY)%'    
    presupuesto:
      mano_obra:
        albañil:
          hasta_1m: 250
          hasta_techo: 300
          reforma_completa: 1200  # NUEVO para baño completo
        fontanero_base: 160
        barra_ducha_extra: 80
        pintura_techo: 70
        mampara_instalacion: 80
        jefe_obra: 100
        minimo: 500
        electricista_base: 150           # NUEVO
        escayola_instalacion: 250        # NUEVO
        radiador_toallero_instalacion: 160 # NUEVO

      materiales:
        plato_ducha:
          hasta_120: 450
          hasta_160: 500
          mayor: 600
        mampara:
          fijo: 400
          fijo_mas_corredera: 800
          angular: 900
          angular_doble: 1200
          ninguna: 0
        griferia:
          estandar: 100
          barra_ducha: 295
          lavabo: 70 
          bide: 70
        azulejos:
          precio_m2: 30
        pavimento:                       # NUEVO para suelo
          precio_m2: 27
        cola:
          base: 50
          extra_techo: 50
          bano_completo: 250
        azulejos_similares_extra: 100
        sanitarios:                      # NUEVO
          ducha: 500
          banera: 450
          bide: 165         # NUEVO
          inodoro: 565
          grupo_higienico: 295
        muebles:
          lavabo:
            60: 475
            80: 670
            100: 1100
          espejo: 295
        radiador_toallero: 370           # NUEVO
        escayola: 60                     # NUEVO
        electricidad:
          foco: 59
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind: 
            $c43Dir: "%kernel.project_dir%/public_html/C43"

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Tests/'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\MisClases\:
        resource: '../src/MisClases/'

    App\MisClases\nordigen:    
        arguments:
            $nordigen: "%kernel.project_dir%/public_html/Nordigen"
            $accept: ""

    App\MisClases\FacturaPdfToJsonService:
        arguments:
            $openaiApiKey: '%openai.api_key%'

    App\MisClases\OpenAIClient:
        arguments:
            $openaiApiKey: '%openai.api_key%'            

    project:
        class: App\MisClases\CestaUser
        arguments: 
            - '@doctrine.orm.entity_manager'

    App\MisClases\FileUploader:
        arguments:
            $targetDirectory: '%kernel.project_dir%/public_html/Light-HTML/img/blog'

    
    #App\EventListener\ExceptionListener:
    #    tags:
    #        - { name: kernel.event_listener, event: kernel.exception }
